export default function groceriesList() {
  const elements = new Map();

  elements.set('Apples', 10);
  elements.set('Tomatoes', 10);
  elements.set('Pasta', 1);
  elements.set('Rice', 1);
  elements.set('Banana', 5);

  return elements;
}
